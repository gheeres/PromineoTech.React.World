const config = {
  baseUrl: `http://localhost:3000`,
  delay: 3000, // 0, // The number of milliseconds to delay requests to simulate slow connections.
  // AuthN / AuthZ
  apiKey: ''
};

export default config;
