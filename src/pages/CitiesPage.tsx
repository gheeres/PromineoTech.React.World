import CountryDropdown from "../components/CountryDropdown";

export default function CitiesPage() {
  console.log(`CitiesPage()`);
  return (
    <>
      <h1>Cities</h1>
      <CountryDropdown />
    </>
  );
}